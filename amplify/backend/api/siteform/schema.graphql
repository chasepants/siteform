type User @model 
@auth(rules: [ {allow: owner } ])
@key(name: "byEmail", fields: ["email", "id"], queryField: "getUserByEmail") {
  id: ID!
  first_name: String!
  last_name: String!
  email: String!
}
